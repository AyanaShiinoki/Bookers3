<div class="container">

	<div class="row">
		<h2 class="profile">Profile</h2>
			<div class="col-xs-4">
				<%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg'%>
			</div>
			<div class="col-xs-8">
				<%= @user.name %><br>
				<%= @user.introduction %>
			</div>
	</div>

	<div class="row">
<!-- books -->
	</div>

</div>
